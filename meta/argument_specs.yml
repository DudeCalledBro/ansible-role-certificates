argument_specs:
  main:
    short_description: certificates
    description: "Install root CA certificates in the trust store."
    author: dudecalledbro
    options:
      certificates:
        type: list
        required: true
        elements: dict
        description: "Define list of certificates to be installed"
